<%= form_with(model: tax_rate) do |form| %>
  <% if tax_rate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tax_rate.errors.count, "error") %> prohibited this tax_rate from being saved:</h2>

      <ul>
        <% tax_rate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :rate, style: "display: block" %>
    <%= form.text_field :rate %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
